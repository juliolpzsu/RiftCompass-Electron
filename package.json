{
  "name": "riftcompass-electron",
  "private": true,
  "version": "0.1.0",
  "description": "RiftCompass desktop companion: champ-select scouting and live in-game coaching, reading the League client locally.",
  "author": "RiftCompass",
  "main": "dist-electron/electron/main.js",
  "overwolf": {
    "packages": ["overlay"]
  },
  "scripts": {
    "dev": "concurrently -k -n VITE,ELECTRON \"npm:dev:renderer\" \"npm:dev:electron\"",
    "dev:renderer": "vite",
    "dev:electron": "wait-on tcp:1421 && tsc -p tsconfig.electron.json && cross-env ELECTRON_RENDERER_URL=http://localhost:1421 electron .",
    "dev:overwolf": "concurrently -k -n VITE,OW-ELECTRON \"npm:dev:renderer\" \"npm:dev:electron:overwolf\"",
    "dev:electron:overwolf": "wait-on tcp:1421 && tsc -p tsconfig.electron.json && cross-env ELECTRON_RENDERER_URL=http://localhost:1421 ow-electron .",
    "build:renderer": "tsc --noEmit && vite build",
    "build:electron": "tsc -p tsconfig.electron.json",
    "build": "npm run build:renderer && npm run build:electron",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.electron.json --noEmit",
    "dist": "npm run build && electron-builder",
    "pack:dir": "npm run build && node scripts/pack-unpacked.mjs",
    "release": "npm run build && electron-builder --publish always",
    "preview": "vite preview"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/modifiers": "^9.0.0",
    "@dnd-kit/sortable": "^10.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "@phosphor-icons/react": "^2.1.10",
    "@primno/dpapi": "^2.0.1",
    "@sentry/electron": "^7.18.0",
    "electron-updater": "^6.8.9",
    "koffi": "^2.9.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@overwolf/ow-electron": "^42.7.1",
    "@overwolf/ow-electron-builder": "^26.9.2",
    "@overwolf/ow-electron-packages-types": "^1.1.10",
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@types/ws": "^8.5.13",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "electron": "^44.0.0",
    "electron-builder": "^26.15.3",
    "typescript": "^5.7.2",
    "vite": "^6.4.3",
    "wait-on": "^8.0.1"
  }
}
